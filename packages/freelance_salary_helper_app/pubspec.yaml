name: freelance_salary_helper_app
description: A new Flutter project.

publish_to: "none" # Remove this line if you wish to publish to pub.dev
version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  flutter_hooks: ^0.10.0
  hooks_riverpod: ^0.2.0
  freezed_annotation:
  json_serializable: ^3.3.0
  intl: ^0.16.1

dev_dependencies:
  flutter_test:
    sdk: flutter

  build_runner:
  freezed: ^0.11.2

flutter:
  uses-material-design: true

  assets:
    - assets/configs/countries_data/
